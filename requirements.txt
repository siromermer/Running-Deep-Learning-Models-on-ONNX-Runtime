# Core dependencies for YOLO and Faster R-CNN inference with ONNX Runtime
torch>=2.8.0
torchvision>=0.23.0
ultralytics>=8.3.0
onnxruntime-gpu>=1.22.0
opencv-python>=4.12.0
numpy>=2.2.0

# Optional: CPU-only ONNX Runtime (use instead of onnxruntime-gpu if no GPU)
# onnxruntime>=1.22.0

# Additional dependencies that might be needed
Pillow>=8.0.0
matplotlib>=3.5.0
tqdm>=4.60.0

# For Jupyter notebook support
jupyter>=1.0.0
ipykernel>=6.0.0

# Additional packages that ultralytics might need
PyYAML>=6.0
requests>=2.25.0
scipy>=1.7.0
pandas>=1.3.0

# For video processing
imageio-ffmpeg>=0.4.0
